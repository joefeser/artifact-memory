# Synthetic private-vault onboarding fixture

This fixture is newly authored synthetic data. It is not copied, transformed,
or redacted from an operator vault, customer system, task transcript, or raw
attachment.

The three records model ordinary operational memory for an invented relay
service. The conformance slice validates the records, builds generated search
views in a disposable workspace, verifies two literal searches, exports a
bounded private-sensitivity context-pack v4, and passes that pack to the
stdlib-only independent reader.

No generated SQLite database or context pack is checked in. Those files are
rebuildable views and may contain private summaries when this workflow is used
with a real external vault.
