# Synthetic Codex-history lifecycle-aware derivative slice

This v2 conformance fixture carries forward the newly authored synthetic v1
inputs. It is not copied, summarized, or redacted from a real Codex task. Its
four derivative records remain draft and require owner review.

The v2 proof negotiates context-pack v4 and verifies that lifecycle-aware
context export suppresses all four drafts, receipts the lifecycle exclusions,
and produces a zero-record informational pack. The released v1 conformance
schema and fixture remain unchanged for strict-reader compatibility.

`expected-receipt.json` is the machine-readable evidence. `receipt.md` is the
same outcome summarized for human review.
