# v0 contract fixtures

These fixtures are synthetic conformance inputs for issues #4, #5, #7, #8,
and #9. The valid record demonstrates distinct meaning, artifact, and content
references. The content digest is for the synthetic bytes
`Artifact Memory synthetic order v0\n`.

`v0-invalid-absolute-path.json` must be rejected: a machine-local absolute path
is an observation detail, not a portable location identity.
