# SyntheticOrders

This intentionally tiny, public-safe C# project exists only to exercise the
Artifact Memory to TraceMap evidence-binding fixture. It contains one `Order`
type, one `Status` property declaration, and one method that reads the
property. It is not derived from production or customer material.
