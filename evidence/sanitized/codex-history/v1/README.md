# Sanitized Codex-history dogfood evidence

This directory contains one operational receipt generated from an explicitly
authorized private single-task import. It is not a synthetic conformance
fixture and contains no task identity, record identity, source or record
digest, source text, private record material, input or output location, raw
transcript, attachment, credential, browser state, or resolver configuration.

The detailed import policy, curated task export, canonical derivative records,
declassification receipt, generated views, and context pack remain in the
owner-controlled private dogfood vault. The public receipt reports only the
operation time, outcome, record-type counts, lifecycle routing, required owner
review state, authority boundary, and explicit nonclaims. It does not
declassify the private material.

The private Git branch and verified local Git bundle were refreshed after the
import. The encrypted archive was not rewritten because its recovery material
is owner-controlled and was neither requested nor accessed. No remote custody
write was attempted.
